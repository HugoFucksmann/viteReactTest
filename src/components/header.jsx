export const HeaderNav = (state) => {
  console.log("que es__ ", state);

  return (
    <div style={{ backgroundColor: "orange", width: "90vw" }}>
      <h3>LARA CLASE REACT !!</h3>
    </div>
  );
};
